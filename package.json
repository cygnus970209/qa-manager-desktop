{
  "name": "qa-manager-desktop",
  "version": "0.1.1",
  "private": true,
  "description": "QA Manager desktop shell (Tauri) — connect to any self-hosted or cloud QA Manager server",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
